#!/usr/bin/env bash
# Qwen3-VL-4B EMA-GRPO baseline for OPD comparisons.
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

source "${SCRIPT_DIR}/../../../../training/common/multi_task_common.sh"

PROJECT_NAME="${PROJECT_NAME:-VideoProxy-baseline-grpo}"
MODEL_PATH="${MODEL_PATH:-/m2v_intern/xuboshen/models/Qwen3-VL-4B-Instruct}"
EXP_NAME="${EXP_NAME:-qwen3_vl_4b_grpo_baseline}"
TASKS="${TASKS:-tg mcq hier_seg event_logic aot}"
TRAINING_MODE="rl"
ADV_ESTIMATOR="${ADV_ESTIMATOR:-ema_grpo}"
DISABLE_KL="${DISABLE_KL:-false}"
USE_KL_LOSS="${USE_KL_LOSS:-true}"
ONLINE_FILTERING="${ONLINE_FILTERING:-true}"
ROLLOUT_N="${ROLLOUT_N:-8}"
ROLLOUT_TEMPERATURE="${ROLLOUT_TEMPERATURE:-1.0}"
LR="${LR:-5e-7}"
KL_COEF="${KL_COEF:-0.01}"
ENTROPY_COEFF="${ENTROPY_COEFF:-0.005}"
CLIP_RATIO_LOW="${CLIP_RATIO_LOW:-0.2}"
CLIP_RATIO_HIGH="${CLIP_RATIO_HIGH:-0.2}"
ROLLOUT_BS="${ROLLOUT_BS:-64}"
GLOBAL_BS="${GLOBAL_BS:-64}"
VAL_BATCH_SIZE="${VAL_BATCH_SIZE:-64}"
ROLLOUT_GPU_MEM_UTIL="${ROLLOUT_GPU_MEM_UTIL:-0.55}"
ROLLOUT_MAX_NUM_SEQS="${ROLLOUT_MAX_NUM_SEQS:-512}"
MB_PER_EXP="${MB_PER_EXP:-2}"
N_GPUS_PER_NODE="${N_GPUS_PER_NODE:-8}"
MAX_FRAMES="${MAX_FRAMES:-256}"
MAX_PIXELS="${MAX_PIXELS:-65536}"
TOTAL_EPOCHS="${TOTAL_EPOCHS:-1}"
SAVE_FREQ="${SAVE_FREQ:-50}"
SAVE_LIMIT="${SAVE_LIMIT:--1}"
VAL_FREQ="${VAL_FREQ:-50}"
VAL_BEFORE_TRAIN="${VAL_BEFORE_TRAIN:-true}"
AOT_TEACHER_MODEL_PATH=""
SEG_TEACHER_MODEL_PATH=""
EVENTLOGIC_TEACHER_MODEL_PATH=""

MIN_ROLLOUT_MAX_BATCHED_TOKENS=$((MAX_PROMPT_LEN + MAX_RESPONSE_LEN))
if (( ROLLOUT_MAX_BATCHED_TOKENS < MIN_ROLLOUT_MAX_BATCHED_TOKENS )); then
    echo "[grpo-baseline] ERROR: ROLLOUT_MAX_BATCHED_TOKENS=${ROLLOUT_MAX_BATCHED_TOKENS} must be >= MAX_PROMPT_LEN + MAX_RESPONSE_LEN = ${MIN_ROLLOUT_MAX_BATCHED_TOKENS}" >&2
    exit 1
fi

source "${SCRIPT_DIR}/../../../../training/launchers/run_multi_task.sh"
