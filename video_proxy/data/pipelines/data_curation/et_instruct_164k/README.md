# ET-Instruct Source Notes

ET-Instruct-specific active code now lives in the shared `../curation/`
package. Use the top-level launcher:

```bash
DATASET=et_instruct_164k bash video_proxy/data/pipelines/data_curation/run.sh
```

This directory is kept only as a source note so historical paths remain easy to
understand.
