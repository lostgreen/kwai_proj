# Temporal AoT Legacy Scripts

Older temporal AoT construction scripts kept for reproducibility and reference.

New code should prefer the active scripts in `video_proxy/data/pipelines/youcook2_seg/temporal_aot/`.
Move a legacy script out only when it becomes part of the active pipeline again.
